import Styled from "styled-components";

export const Container = Styled.div`
  ${() =>`
    display: flex;
    flex: 1;
    align-items: center;
    justify-content: center;
    gap: .5rem;
  `}
`;